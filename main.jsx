 
import React from "react";
import ReactDOM from "react-dom/client";
import WeightTracker from "./App.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <WeightTracker />
  </React.StrictMode>
);